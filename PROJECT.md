<!-- DOC_BADGE: OPERATIONAL -->
<!-- DOC_STATUS: ACTIVE_BOUNDED_MISSION -->
<!-- DOC_LAST_VERIFIED: 2026-07-15 -->

# Active bounded mission

Roadmap item: R-07 — run a read-only no-repeat ownership and contract audit of
fail-closed Qdrant observation after the authenticated protected-membership
composition checkpoint.

## Outcome

Determine whether one Qdrant observation authority already exists, how current
candidate-plan contracts expect exact passive Qdrant evidence to enter
`ResolvedCleanupScope`, and what smallest unfinished seam remains. Produce only
read-only ownership/contract evidence and a decision. Do not implement, contact
live Qdrant, or open planning, approval, or cleanup.

## Completed work — do not repeat

- Configuration projection, immutable candidate planning, cleanup-target
  filesystem observation, protected-manifest validation, protected-membership
  projection, shared held-handle traversal, Windows security mechanics, reader
  identity, external-pin reading, authenticated protected-manifest reading, and
  the shared ProgramData locator are checkpointed.
- `9e225655` adds the selected Windows protected-boundary observer and exact
  synthetic regression authority.
- `636f4bfd` closes the final observer error-code deletion/rebinding bypass
  without changing its public API or authority.
- The final observer source has exactly three exports, accepts only direct
  membership and external-pin evidence, derives five pin identities internally,
  retains all physical evidence through one final fence, and returns the
  existing 18-role `ProtectedBoundaryEvidence` tuple.
- Fresh verification passed 184 focused tests, a 1,155-test bounded union, and
  the expanded 1,807-test zero-drop gate on an unchanged retry. Independent
  final review returned specification compliant with no remaining finding.
- The pre-existing external-pin module-reload test-isolation defect has a
  separate roadmap owner. Do not weaken production exact-type checks or repair
  that test inside this mission.
- The composition recheck found one singular acyclic owner graph and selected
  the exact existing source/test pair, private helper signature, four locator
  fences, digest chain, lexical exclusion, five private errors, and direct RED
  matrix. Do not repeat that audit or create a new composition module.
- `d20a74ba` implements and checkpoints that composition authority in exactly
  the selected source/test pair. Final verification passed 265 focused tests,
  the exact 1,941-test zero-drop authority union, the 375-test shared authority
  gate, static/documentation gates, and two independent exact-hash reviews.
- The helper preserves the three-symbol public API, exact direct-output and
  final-fence contract, same-object 18-role tuple return, and path-free public
  error behavior. Do not reopen or recreate this seam.

## Governing invariant

Qdrant evidence is passive candidate-plan pre-state. It may not authenticate
protected membership, perform mutation, create a plan, or become a second
configuration authority. The audit must identify one owner and exact evidence
contract before implementation is authorized.

Authenticated protected composition and cleanup-target filesystem observation
are completed prerequisites. Scope assembly, planning, persistence, approval,
jobs/tokens, and cleanup remain later seams.

## Exact audit seam

1. reconcile current instructions, source, tests, diagnostics, and Git/worktree
   state before making any claim;
2. trace every existing Qdrant evidence type, observer, adapter, candidate-plan
   input, and test authority without contacting a live service;
3. distinguish passive observation from scope assembly, planning, persistence,
   approval, and execution;
4. identify whether one implementation owner already exists or document the
   exact missing boundary; and
5. record a no-repeat decision in one diagnostic plus the sole roadmap. Make no
   production or test change during the audit.

## Governing evidence

- `docs/diagnostics/R07_AUTHENTICATED_PROTECTED_MEMBERSHIP_COMPOSITION_AUDIT_2026-07-14.md`;
- `docs/diagnostics/R07_AUTHENTICATED_PROTECTED_MEMBERSHIP_COMPOSITION_RECHECK_DECISION_2026-07-15.md`;
- `docs/diagnostics/R07_AUTHENTICATED_PROTECTED_MEMBERSHIP_COMPOSITION_CHECKPOINT_2026-07-15.md`;
- `docs/diagnostics/R07_PROTECTED_BOUNDARY_OBSERVER_CHECKPOINT_2026-07-15.md`;
- `docs/diagnostics/R07_PROTECTED_BOUNDARY_OBSERVER_CONTRACT_DECISION_2026-07-15.md`;
- `docs/diagnostics/R07_WINDOWS_PROGRAM_DATA_LOCATOR_CHECKPOINT_2026-07-14.md`;
- `docs/diagnostics/R07_PROTECTED_MANIFEST_READER_CHECKPOINT_2026-07-14.md`;
- `docs/diagnostics/R07_WINDOWS_EXTERNAL_PIN_READER_CHECKPOINT_2026-07-14.md`;
- their closed source/tests; and
- `docs/releases/ROADMAP.md`.

## Boundaries

- Work only in `.worktrees/r05-api-authority` on
  `codex/r05-api-authority`.
- Read repository source, tests, diagnostics, and Git/worktree evidence only.
  Do not modify production or tests during the audit.
- Do not inspect or mutate live ProgramData, a token, ACL, descriptor,
  configured or protected root, manifest, pin, service, GoodQ data, Qdrant,
  evidence store, job, MiniAgent, approval, or cleanup target.
- Do not contact Qdrant, observe cleanup targets, assemble a resolved scope,
  build or persist a plan, load configuration, add a command surface, issue
  approval, create jobs/tokens, control processes, or execute cleanup.

## Completion gate

1. one current-byte ownership map covers Qdrant evidence types, observation
   entrypoints, candidate-plan consumers, and test authorities;
2. completed work and no-repeat boundaries are explicit;
3. passive observation, service contact, scope assembly, planning, persistence,
   approval, and execution authorities are separated;
4. risks and missing evidence are classified without live Qdrant access;
5. one smallest coherent next seam and exact allowlist are selected, or the
   audit records why implementation must remain closed; and
6. the decision and sole roadmap are checkpointed separately from future code.
