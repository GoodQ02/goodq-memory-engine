from fastapi import APIRouter

# Retired compatibility placeholder.
# The old /runs index surface is no longer mounted because its backing
# run-index module is gone and the remaining runtime truth lives elsewhere.
router = APIRouter()
