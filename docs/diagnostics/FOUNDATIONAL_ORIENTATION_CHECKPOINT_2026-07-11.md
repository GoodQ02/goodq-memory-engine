<!-- DOC_BADGE: OPERATIONAL -->
<!-- DOC_STATUS: CHECKPOINT_EVIDENCE_COMPLETE -->
<!-- DOC_LAST_VERIFIED: 2026-07-11 -->

# Foundational Project Orientation Checkpoint Evidence

## Purpose

Fresh agents need durable topology and authority before interpreting transient
state. This checkpoint preserves the completed project-orientation and
instruction-alignment seam without importing current runtime claims, mixed-tree
implementation work, or machine-specific control-plane files.

## Checkpoint Lineage

- Branch: `codex/r17-foundational-docs`
- R-06 evidence base: `b4ed9f3c`
- Documentation checkpoint: `ac4f58f6 docs: establish timeless project orientation`
- Status: independently reviewed, verified, and privately checkpointed

## Included Surface

- timeless multi-root project orientation and question-scoped authority model
- mandatory no-repeat preflight and shift-report structure
- orientation-first root and Agent Office reading order
- stable Agent Knowledge Workspace shortcut resolution
- private-development/public-release branch authority
- identical first-read insertion in both repository operator-skill copies
- operational/transient classification in the documentation authority map
- Gemini guide authority boundary
- completed orientation plan archived with canonical historical metadata

## Explicit Boundaries

- `PROJECT.md`, current-state Markdown/JSON, generated status, UCF reports,
  broader archive moves, indexes, and runtime code were not included.
- The two operator-skill copies were not otherwise reconciled; R-07 owns the
  clean-memory workflow and R-13 owns later authority/index consolidation.
- Workstation and Hermes instruction files remain machine-local. Their existing
  cross-root alignment was verified through the Agent Knowledge Workspace, not
  copied into this repository checkpoint.
- The frozen mixed main checkout remained unchanged at 85 entries.

## Verification Evidence

- Orientation timelessness scan found no live epoch, operational date, loopback
  endpoint, hostname, or literal drive root below metadata.
- All relative Markdown links in the eight-file seam resolve.
- Banned-token and cached-diff checks passed.
- The targeted agent-workspace governance test passed.
- The Agent Knowledge Workspace shortcut resolved and its verifier passed.
- The completed active plan path is absent; its canonical archived path exists.
- Independent read-only review found no remaining scope, authority, metadata,
  link, or path blocker.
- Full documentation drift still reports the pre-existing graph-audit literal
  paths assigned to R-13; this seam added no new violation.

## Public-Hook Finding

The private repository has a local post-commit hook that automatically copies
and stages `AGENTS.md` in the public checkout. It fired during this checkpoint,
contradicting the explicit public-release approval gate. The exact public
working-tree side effect was reversed immediately; the public checkout's
pre-existing commit and branch history were not changed.

The hook is now recorded as R-16 work. Future private checkpoints that include
`AGENTS.md` must not rely on that automatic release behavior.

## Resume

Continue R-17 inventory of remaining mixed-main families. Do not rebuild this
orientation seam. Current-state truth remains R-09 and documentation authority,
archive, index, and naming consolidation remains R-13.
