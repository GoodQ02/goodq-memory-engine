<!-- DOC_BADGE: OPERATIONAL -->
<!-- DOC_STATUS: CHECKPOINT_EVIDENCE_COMPLETE -->
<!-- DOC_LAST_VERIFIED: 2026-07-15 -->

# R-07 Authenticated Protected-Membership Composition Checkpoint

## Outcome

The selected private authenticated protected-membership composition authority
is implemented and checkpointed in one source commit:

```text
d20a74ba feat(clean-memory): compose authenticated protected membership
```

The implementation changes only the existing configuration module and its
focused test authority. It preserves the exact three-symbol public API, adds
one private closed error and one private composition helper, invokes the
selected public dependency chain once in the frozen order, and returns the
observer's same exact 18-role evidence tuple.

It adds no command, Qdrant, scope assembly, plan persistence, approval, job,
token, process-control, cleanup, logging, network, or output capability.

## Closed Contract

- Exact Windows-flavor configuration is authenticated before capability
  construction, including the derived failed, import, and processed media
  bindings.
- Public dependencies remain function-local and the module's public API remains
  exactly `CONFIGURATION_SCHEMA`, `ResolvedPlanConfiguration`, and
  `resolve_plan_configuration`.
- ProgramData is acquired through one retained locator at four exact fences.
  Drive, ProgramData, fixed-directory, and pin-name components must remain
  exact, separator-free, and control-free.
- The external pin, protected manifest, membership projection, and protected
  boundary observer are called once in the selected order. Every direct return
  is exact-type and structurally authenticated before it is forwarded.
- Configuration, pin, manifest, membership, and boundary digests remain bound
  through final return. Equal-value distinct manifest bytes fail the exact
  final identity fence.
- Lexical pin-chain exclusion uses complete canonical Windows components only;
  the protected observer remains the sole physical-alias authority.
- Upstream drift wins over malformed-result classification at every later
  dependency stage.
- Dependency-owned public errors preserve their exact outer object and
  traceback only after their private code is validated, their canonical
  code-bound message arguments are restored, and their cause, context, notes,
  and dynamic attributes are replaced or removed. Unknown, missing, or
  non-string codes fail closed as path-free `composition_failed`.
- The final fence rechecks all retained direct evidence and returns the same
  exact observer tuple without copy, wrapper, digest, reorder, or partial
  result.

## TDD And Review Hardening

The selected lifecycle and negative-mutant matrix was implemented test-first.
Two later bounded RED receipts were retained rather than explained away:

- the derived-path, detached-component, and public-error-graph correction batch
  produced 13 expected failures and 13 already-passing controls before the
  production correction; and
- the final security review found that forged dependency-owned `args` could
  still render raw detail. The six accepted stages all failed the new canonical
  argument oracle before the source correction and passed afterward.

The final invalid-code matrix rotates unknown, non-string, and missing private
codes across all six accepted stages. It proves fail-closed behavior without
linking or rendering the malformed dependency error.

Both independent reviewers re-read the exact final hashes. The contract/oracle
review and the security/capability review returned `APPROVED` with no remaining
finding.

## Fresh Verification

All Python commands used the explicit `goodq_core` interpreter. All capability
surfaces were synthetic, fake, or in-memory.

| Gate | Result |
| --- | ---: |
| Dependency-owned public-error matrix | 18 passed |
| Full focused configuration/composition file | 265 passed |
| Exact 12-file zero-drop authority union | 1,941 passed |
| MiniAgent/action-job/shared-authority gate | 375 passed |
| Exact two-file Python compilation | passed |
| Public API census | exactly 3 unchanged symbols |
| Private helper/error census | exactly 1 each |
| Documentation authority and generated-index verification | passed |
| Semantic documentation drift lint | 358 files, 0 active violations |
| Banned-token lint | passed |
| Dependency-drift lint | passed |
| Exact two-file diff and whitespace checks | passed |
| Independent contract/oracle review | APPROVED |
| Independent security/capability review | APPROVED |

An earlier pre-final 375-test run retained one unrelated synthetic concurrency
witness failure in passive-latest observation. The isolated test passed, its
unchanged retry passed all 375 tests, and the current exact source later passed
the same 375-test gate directly. No production or test change was made for that
transient receipt.

The reviewed committed SHA-256 hashes are:

```text
cli/clean_memory.py
286042E882D4828A970418CAAC4CC1E7C6D2783CF1CB09111421626781FD0A06

tests/unit/test_clean_memory_cli.py
E5E9B3B987726B9FC204F4FA9FCB10F979E450B81259980BE84699DE248E470B
```

## Evidence Boundary

No implementation, test, review, or verification read or changed live
ProgramData, a production pin or manifest, token, ACL, descriptor, configured
or protected root, service, GoodQ data, Qdrant store, evidence store, job,
MiniAgent, approval state, cleanup target, or cleanup operation.

The mixed main checkout remained frozen. Nothing was pushed or merged.

## No-Repeat Boundary

Do not recreate this composition helper, add a second composition module,
export the helper/error, accept caller paths or dependency injection, weaken
exact direct-output or final-fence checks, duplicate lexical or physical
authority, or fold Qdrant, planning, approval, or cleanup into this helper.

## Next Bounded Mission

Run one read-only no-repeat ownership and contract audit of fail-closed Qdrant
observation. Determine whether one implementation authority already exists,
how it supplies exact passive evidence to later `ResolvedCleanupScope`
assembly, and what smallest seam remains. Do not implement or contact live
Qdrant during that audit.

Runnable planning, persistence, command parsing, approval, jobs/tokens, process
control, and cleanup execution remain closed.
