name: goodq_core
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - pyyaml
  - python-dotenv
  - requests
  - fastapi
  - uvicorn
  - typer
  - pytorch
  - torchvision
  - torchaudio
  - imageio-ffmpeg
  - pip:
      - -e .
      - transformers
      - sentence-transformers
      - qdrant-client
      - faster-whisper
#
# Public baseline environment only.
# Specialized step environments remain under envs/*/requirements.txt
# and envs/locks/*.lock.txt for GPU/WSL and model-specific workloads.
