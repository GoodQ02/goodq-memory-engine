# Bootstrap installs Conda dlib separately for Windows reproducibility, then
# applies the locked YuNet/SFace primary plus dlib fallback stack.
setuptools==80.10.2
face-recognition==1.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.2.0
